enum Method { get, post, put, delete }

enum AppType { debug, release }

enum StateType { loading, empty, error, success }

AppType appType = AppType.debug;

String pixabayPhotoUrl = "https://pixabay.com/api/";
